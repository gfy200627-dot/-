# 复制为 .env.local 后按需修改（.env.local 优先级最高，且不会被 git 跟踪）

# 后端 API 基地址
# 本地 FastAPI：http://127.0.0.1:8000/api
# 同域部署：/api（配合 nginx 反向代理）
VITE_API_BASE_URL=/api

# true = 前端内置 Mock 服务；false = 请求真实后端
VITE_USE_MOCK=true

# Mock 网络延迟（ms）
VITE_MOCK_DELAY=260

VITE_APP_TITLE=AutoInsight
